## 1. Implementation
- [x] 1.1 抽取前端搭配预览兼容性判断辅助函数，集中定义女性裙装识别与预览兼容判定
- [x] 1.2 在搭配页下衣列表和相关选择路径中应用兼容过滤，确保男模特不再看到女性裙装候选
- [x] 1.3 在搭配页生成动作中增加兼容性拦截与提示，阻止旧状态或缓存状态提交不兼容组合
- [x] 1.4 为后端预览接口补充兼容性元数据校验，并在 workflow 调用前拒绝不兼容请求
- [x] 1.5 为 unified-agent 预览工具增加相同兼容性规则，阻止通过 Agent 生成不兼容预览图
- [x] 1.6 为前端、后端和 Agent 补充回归测试，覆盖允许与拒绝两类场景

## 2. Validation
- [x] 2.1 运行前端相关测试，验证搭配页过滤与生成拦截
- [x] 2.2 运行后端与 Agent 相关测试，验证接口和工具层拒绝逻辑
- [x] 2.3 运行 `openspec validate update-match-preview-gender-compatibility --strict`
