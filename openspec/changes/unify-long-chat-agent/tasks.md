## 1. Spec & Validation
- [ ] 1.1 明确统一 Agent 的会话、消息、摘要与长期偏好边界
- [ ] 1.2 编写 `unified-agent` spec delta，覆盖会话恢复、长对话记忆、统一规划与确认机制
- [ ] 1.3 运行 `openspec validate unify-long-chat-agent --strict`

## 2. Database
- [ ] 2.1 新增 `agent_sessions` 表
- [ ] 2.2 新增 `agent_messages` 表
- [ ] 2.3 新增 `agent_session_memory` 表
- [ ] 2.4 定义会话与消息的索引、恢复查询和摘要更新字段

## 3. Backend
- [ ] 3.1 新增会话存储层：会话创建、会话列表、会话恢复接口
- [ ] 3.2 新增消息持久化层：消息写入、最近 12 轮读取、消息类型区分
- [ ] 3.3 新增会话摘要层：摘要生成、`last_summarized_message_id` 更新、恢复契约组装
- [ ] 3.4 统一 `/chat` 与 `/agent` 的上下文组装与 LLM 规划逻辑
- [ ] 3.5 在统一对话流中接入任务规划、工具调用与确认门控
- [ ] 3.6 将长期偏好摘要注入统一 Agent 上下文
- [ ] 3.7 设计旧 `/chat` 兼容层与收口逻辑，明确是否迁移旧历史

## 4. Frontend
- [ ] 4.1 统一 Agent 页面支持会话列表与历史恢复
- [ ] 4.2 统一展示长对话消息、任务理解卡片、确认卡片和工具结果
- [ ] 4.3 将快捷入口统一跳转到长对话式 Agent 页面
- [ ] 4.4 逐步将旧 `/aichat` 迁移为兼容入口或跳转

## 5. Verification
- [ ] 5.1 手工验证：打开旧会话后可恢复最近 12 轮消息与摘要上下文
- [ ] 5.2 手工验证：在统一会话中连续对话后仍可继续执行任务
- [ ] 5.3 手工验证：写操作在统一会话中仍能进入确认流程
- [ ] 5.4 手工验证：跨页面快捷入口都能携带上下文进入统一 Agent
